apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: app

  labels:
    app: backend
    component: backend

spec:

  type: ClusterIP

  selector:
    app: backend

  ports:

    - name: http

      port: 80

      targetPort: 3000

      protocol: TCP